import $ from 'jquery'

let config = {
  html4Mode: false,
  timeOut: 30000,
  parent: $('body'),
  routes: {},
  resource: {},
  error: {},
  debug: false,
  animation: false,
  templateApi: '',
  fixDirection: false,
  animationEnd: window.WebKitAnimationEvent ? 'webkitAnimationEnd' : (window.AnimationEvent ? 'animationend' : ''),
  transitionEnd: window.WebKitTransitionEvent ? 'webkitTransitionEnd' : (window.TransitionEvent ? 'transitionend' : ''),
  isWebkit: 'WebkitAppearance' in document.documentElement.style || typeof document.webkitHidden !== 'undefined',
  defaultScrollTop: 0
}

export default config
