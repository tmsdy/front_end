export const set_customerInfo = function ({ commit, state }) {
    commit(types.SET_CUSTOMERINFO, {})
}
export const set_routParameters = function ({ commit, state }) {
    commit(types.SET_ROUTPARAMETERS, {})
}
